--- src/qt/bitcoingui.cpp
+++ src/qt/bitcoingui.cpp
@@ -117,7 +120,7 @@ BitcoinGUI::BitcoinGUI(const NetworkStyle* networkStyle, QWidget* parent) : QMai
 
     GUIUtil::restoreWindowGeometry("nWindow", QSize(880, 550), this);
 
-    QString windowTitle = tr("VGS Lasvegascoin Core - Wallet 3.0.0.13") + " - ";
+    QString windowTitle = tr("VGS Lasvegascoin Core - Wallet 3.0.0.14") + " - ";
 #ifdef ENABLE_WALLET
     /* if compiled with wallet support, -disablewallet can still disable the wallet */
     enableWallet = !GetBoolArg("-disablewallet", false);
